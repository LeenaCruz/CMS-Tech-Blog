{{#if loggedIn}}
<div>
    {{#each blogPosts as |blogpost|}}
    {{> blogpost_details}}
    {{/each}}
    <div class="form-group">
        <button class="btn btn-primary" type="submit"><a href="/blogpost">Create Post</a></button>
    </div>
</div>
{{else}}
<p>Please log in to view your dashboard.</p>
<div>
    <a href="/login">Login Here</a>
</div>
{{/if}}